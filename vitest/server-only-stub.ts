// Vitest stub for the `server-only` directive module.
// The real package throws when loaded outside a server bundle. In the
// test environment we want server modules to load transparently.
export {};

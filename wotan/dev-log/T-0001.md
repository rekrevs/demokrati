# T-0001: Create GitHub repo rekrevs/demokrati and configure remote

**Status**: BLOCKED | **Size**: S

## What
Create public GitHub repo `rekrevs/demokrati` via `gh repo create`, add as remote to local repo at `~/repos/demokrati`, and push `main`.

## Blocker
`gh auth status` reports invalid token. User must run `gh auth login -h github.com` interactively.

## Log

## Outcome
